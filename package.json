{
  "name": "shopco",
  "private": true,
  "type": "module",
  "scripts": {
    "build:all": "cd client && npm install && npm run build && cd ../admin && npm install && npm run build",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\" \"cd admin && npm run dev\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "firebase-admin": "^14.4.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}