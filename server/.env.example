# Server
PORT=4000

# Admin credentials
ADMIN_EMAIL=admin.shopco@gmail.com
ADMIN_PASSWORD=123456789

# Firebase config
FIREBASE_PROJECT_ID=shopcoanddashbord
FIREBASE_STORAGE_BUCKET=shopcoanddashbord.firebasestorage.app
FIREBASE_SERVICE_ACCOUNT_PATH=firebase-service-account.json

# For Vercel deployment: base64-encode your service account JSON and put it here
# Generate it with: base64 firebase-service-account.json (on Mac/Linux)
# Or on Windows: [Convert]::ToBase64String([IO.File]::ReadAllBytes("firebase-service-account.json"))
FIREBASE_SERVICE_ACCOUNT_B64=